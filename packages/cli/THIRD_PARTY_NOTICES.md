# Third-party notices for @imposia/cli

This package declares @imposia/node as a separate workspace/runtime dependency. The CLI tarball does not bundle that package or its transitive dependencies. Installations receive @imposia/node's own LICENSE and THIRD_PARTY_NOTICES.md alongside the dependency; this package preserves the direct workspace attribution here rather than claiming inherited source.

## @imposia/node 0.1.0 — Apache-2.0 (separate dependency)

See the installed @imposia/node package's LICENSE and THIRD_PARTY_NOTICES.md for its direct dependency notices (including pdfjs-dist and Playwright).
