# Third-party notices for @imposia/viewer

This package depends on `@imposia/core` for its public page-document types. Core source is not bundled into this tarball, and the package has no other runtime dependency.
